{
    "title": "Package Health Check Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "packages": {
            "title": "Packages",
            "type": "array",
            "editor": "json",
            "description": "List of packages to check: [{ \"ecosystem\": \"npm\", \"name\": \"left-pad\" }]. Supported ecosystems: npm, pypi, crates, rubygems, packagist, nuget.",
            "prefill": [
                { "ecosystem": "npm", "name": "express" }
            ]
        },
        "githubToken": {
            "title": "GitHub token (optional)",
            "type": "string",
            "editor": "textfield",
            "isSecret": true,
            "description": "Optional personal access token to raise the GitHub API rate limit from 60 to 5,000 requests/hour when checking linked repos in bulk. Leave blank for small batches."
        }
    },
    "required": ["packages"]
}
